클래스 상속
프로그램에서는 자식이 부모를 선택한다. extends

public class 자식 클래스 extends 부모 클래스

자바는 다중 상속을 허용하지 않는다.

public class 자식 클래스 extends 부모 클래스 implements 인테페이스 1,인테페이스 2.(기능은 인테페이스를 이용하여 다중상속을 구현한다.)


2. 부모 생성자 호출
  super() 컴파일과정에서 자동 추가된다.이것은 부모의 기본 생성자를 호출한다.
  만약 부모 클래스에 기본 생성자가 없으면 자식생성자가 생성이 불가하다


3. 메소드 재정의(메소드 오버라이딩)

  상속된 메소드를 자식 클래스에서 새롭게 재정의를 하는 것

  우리한테 메소드 오버라이딩을 왜 쓸까?
   부모클래스에게 받는 메소드를 약간 변형해서 사용하면


final 클래스, final 메소드

final 필드 : 초기값 설정 후 값을 변경할 수 없다.
final 클래스 : 클래스 선언 시 final를 클래스 앞에 붙이면 최종적인 클래스이므로 더 이상 상속X

final 메소드 : 





